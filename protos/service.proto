syntax = "proto3";

package grpc;

message Empty {}

message User {
    int64 uuid = 1;
    string username = 2;
}

message Chat {
    int64 send_id = 1;
    int64 receive_id = 2;
    string message = 3;
}

message Outcome {
    int64 err_type = 1;
    string err_msg = 2;
}

service ChatBot {
    rpc server_send_chat (Chat) returns (Outcome);
    rpc server_get_chat (User) returns (Chat); 
}